@extends('layouts.app')
@section('title','Detail Pengaduan')
@section('page_title','Detail Pengaduan')
@section('content')
<div class="row g-4"><div class="col-lg-7"><div class="content-card p-4"><div class="d-flex justify-content-between"><h4 class="fw-bold">{{ $complaint->title }}</h4>@include('partials.status-badge',['status'=>$complaint->status])</div><p class="text-muted">{{ $complaint->category->category_name }} • {{ $complaint->complaint_date->format('d/m/Y') }}</p>@if($complaint->photo)<img src="{{ asset('storage/'.$complaint->photo) }}" class="img-fluid rounded-4 mb-3" alt="Foto bukti">@endif<p>{{ $complaint->description }}</p><hr><h6>Lokasi</h6><p class="mb-1">{{ $complaint->address }}</p><p class="text-muted">{{ $complaint->village_name }}, {{ $complaint->district_name }}, {{ $complaint->regency_name }}, {{ $complaint->province_name }}</p><div id="map-picker" class="map-picker"></div><input type="hidden" id="latitude" value="{{ $complaint->latitude }}"><input type="hidden" id="longitude" value="{{ $complaint->longitude }}"></div></div><div class="col-lg-5"><div class="content-card p-4"><h5 class="fw-bold mb-3">Riwayat Tanggapan Admin</h5>@forelse($complaint->responses as $response)<div class="border-start border-3 ps-3 mb-3" style="border-color: var(--coral)!important"><div class="fw-semibold">{{ $response->admin->name }}</div><small class="text-muted">{{ $response->response_date->format('d/m/Y') }}</small><p class="mb-0 mt-2">{{ $response->response }}</p></div>@empty<p class="text-muted">Belum ada tanggapan.</p>@endforelse</div></div></div>
@endsection
@push('scripts')<script>document.addEventListener('DOMContentLoaded',()=>window.initLocationPicker({latitude:@json($complaint->latitude),longitude:@json($complaint->longitude)}));</script>@endpush
