@extends('layouts.app')
@section('title','Pengaduan Saya')
@section('page_title','Pengaduan Saya')
@section('content')
<div class="d-flex justify-content-between mb-3"><h5 class="fw-bold">Daftar Pengaduan Saya</h5><a class="btn btn-coral" href="{{ route('masyarakat.complaints.create') }}">Buat Pengaduan</a></div>
<div class="content-card p-4 mb-3"><form class="row g-2"><div class="col-md-4"><input class="form-control" name="search" value="{{ request('search') }}" placeholder="Cari judul/deskripsi"></div><div class="col-md-3"><select class="form-select" name="category_id"><option value="">Semua kategori</option>@foreach($categories as $category)<option value="{{ $category->id }}" @selected(request('category_id')==$category->id)>{{ $category->category_name }}</option>@endforeach</select></div><div class="col-md-3"><select class="form-select" name="status"><option value="">Semua status</option>@foreach($statuses as $status)<option value="{{ $status }}" @selected(request('status')===$status)>{{ $status }}</option>@endforeach</select></div><div class="col-md-2"><button class="btn btn-coral w-100">Filter</button></div></form></div>
<div class="content-card p-4"><div class="table-responsive"><table class="table table-hover"><thead><tr><th>Judul</th><th>Kategori</th><th>Lokasi</th><th>Status</th><th>Tanggal</th><th>Aksi</th></tr></thead><tbody>@forelse($complaints as $complaint)<tr><td>{{ $complaint->title }}</td><td>{{ $complaint->category->category_name }}</td><td>{{ $complaint->regency_name }}, {{ $complaint->province_name }}</td><td>@include('partials.status-badge',['status'=>$complaint->status])</td><td>{{ $complaint->complaint_date->format('d/m/Y') }}</td><td class="d-flex gap-2"><a class="btn btn-sm btn-outline-secondary" href="{{ route('masyarakat.complaints.show',$complaint) }}">Detail</a>@if($complaint->status === \App\Models\Complaint::STATUS_WAITING)<a class="btn btn-sm btn-outline-primary" href="{{ route('masyarakat.complaints.edit',$complaint) }}">Edit</a><form method="POST" action="{{ route('masyarakat.complaints.destroy',$complaint) }}">@csrf @method('DELETE')<button type="button" onclick="confirmDelete(this.form)" class="btn btn-sm btn-outline-danger">Hapus</button></form>@endif</td></tr>@empty<tr><td colspan="6" class="text-center text-muted">Belum ada data.</td></tr>@endforelse</tbody></table></div>{{ $complaints->links() }}</div>
@endsection
